import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

/*

TODO:
Spinner wheel for loading screen
backgroudn picture/info when there are no chrores recorded
trash bin for deleted chores
Side menu / hambuerger menu
set booleans for each botton
homepage
save theme on local storage
input for rename, 
alert for errors like not found, repeated chore

Translate to Spanish
Mobil version

ENV variables

Include reset.css








*/
